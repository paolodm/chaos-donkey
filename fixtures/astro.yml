# This file is being watched by the magnus. Any changes will result in a restart of
# the proxy.

# The site you want to proxy
origin: localhost:8080

# Responses must include the whole url, including querystrings
responses:
  - url: /ei/app/questionnaire_status?customer=2023561
    statusCode: 500
  - url: /ei/app/questionnaire/cooling_type.json
    statusCode: 500
  - url: /ei/app/modules/customer/2023561/disaggregation?
    statusCode: 500
